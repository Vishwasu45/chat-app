spring:
  ai:
    openai.api-key: xxxxxxxxxxxxxxxxx
  application:
    name: News-Aggregator
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/chat_db
    username: postgres
    password: root

chat:
  client:
    key: xxxxxxxxxxxxxxxx